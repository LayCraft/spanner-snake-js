// return a list of priorities ranked from most important to least important based on snake stats.
const priorities = (blob) => {
    // food? Avoid collisions? I don't know how this works yet.
    return [];
}

module.export = priorities;